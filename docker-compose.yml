# This file is deprecated as of v7.0.
# The new, unified architecture simplifies local development.
# To run the application locally, please run the following command from the project root:
#
#   npm run dev
#
# This will start both the frontend and backend services concurrently with hot-reloading.
